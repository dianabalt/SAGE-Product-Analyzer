# Supabase Configuration
# Get these values from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# OpenAI Configuration (server-side only - never exposed to client)
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Tavily Search API (optional - used for web research fallback when DOM extraction fails)
# Get API key from: https://tavily.com
# Free tier: 1,000 searches/month
# Only used ~30% of the time (when product page doesn't have structured ingredients)
SEARCH_API_KEY=tvly-your-tavily-api-key-here

# Development
# NODE_ENV=development
